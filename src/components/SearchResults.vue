<template>
  <ul>
  </ul>
</template>

<script lang="ts">

</script>

<!-- // generateKey(item: Character | House | Quote): string {
//       const currentDate = Date.now();
//       const randomPart = Math.random();
//       return `${this.getItemName(item)}-${currentDate}-${randomPart}`;
//     },
//     getItemName(item: Character | House | Quote): string {
//       if ('name' in item) {
//         return item.name;
//       } else if ('character' in item && 'name' in item.character) {
//         return item.character.name;
//       } else {
//         return 'Unknown';
//       }
//     },
//    -->

<!-- // <li v-for="item in items" :key="generateKey(item)">{{ getItemName(item) }}</li> -->
